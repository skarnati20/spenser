[package]
name = "git"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
types  = { path = "../types" }
core   = { path = "../core" }
anyhow = "1"
git2 = { version = "0.19", features = ["vendored-libgit2", "vendored-openssl"] }
serde_json = "1"